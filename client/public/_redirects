# Static assets should be served as-is
# Handle nested static asset paths by redirecting to the correct location
/*/static/js/* /static/js/:splat 200
/*/static/css/* /static/css/:splat 200
/*/static/media/* /static/media/:splat 200

# Traditional static folder routing
/static/* /static/:splat 200
/manifest.json /manifest.json 200
/favicon.ico /favicon.ico 200
/sw.js /sw.js 200

# API routes should not be redirected (if any)
/api/*  https://product-traceability-api.onrender.com/api/:splat  200

# SPA routing - redirect all other routes to index.html
/*    /index.html   200
