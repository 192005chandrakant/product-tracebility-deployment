{
  "scripts": {
    "build": "npm run build:frontend && npm --prefix server run build",
    "build:frontend": "npm --prefix client run build:prod",
    "build:frontend:clean": "npm --prefix client run build:clean",
    "build:frontend:deploy": "npm --prefix client run build:deploy",
    "build:prod": "npm run build:frontend && npm --prefix server run prod",
    "preview:frontend": "npm --prefix client run preview",
    "deploy-contract": "npx hardhat run scripts/deploy.js --network sepolia",
    "verify-contract": "npx hardhat verify --network sepolia",
    "test-blockchain": "node test-blockchain-connection.js",
    "validate-deployment": "node validate-deployment.js",
    "setup-deployment": "node setup-for-deployment.bat",
    "prestart": "npm --prefix server run build && npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "jwt-decode": "^4.0.0",
    "mongoose": "^8.16.0",
    "multer": "^2.0.1",
    "node-fetch": "^3.3.2",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "dotenv": "^16.5.0",
    "ethers": "^5.7.2",
    "hardhat": "^2.22.2"
  }
}
